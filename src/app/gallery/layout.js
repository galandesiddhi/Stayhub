export const metadata = {
  title: "Photo Gallery | SOL Stay",
  description: "Browse actual photos of SOL Stay bedrooms, co-working suites, kitchens, and green rooftop spaces.",
};

export default function GalleryLayout({ children }) {
  return <>{children}</>;
}
