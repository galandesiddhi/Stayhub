export const metadata = {
  title: "Photo Gallery | StayHub",
  description: "Browse actual photos of StayHub bedrooms, co-working suites, kitchens, and green rooftop spaces.",
};

export default function GalleryLayout({ children }) {
  return <>{children}</>;
}
